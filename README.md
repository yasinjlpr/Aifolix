# Aifolix

**Convert AI-generated folder structures into ready-to-run shell scripts.**

Aifolix is a lightweight tool that converts a folder/file structure generated by AI into a script that can automatically create the entire project structure for you.

Instead of manually creating dozens of folders and files, simply paste the structure, choose your preferred shell, and generate the script.

>  **Note:** The current version of Aifolix is available only for Windows.  
> However, inside the app you can generate scripts for **CMD**, **PowerShell (.ps1)**, **Bash (.sh)**, and **Zsh (.zsh)** — so you can create project-structure scripts for other platforms from your Windows machine.

##  Features

- Convert AI-generated folder structures into executable scripts
- Support for Windows (CMD & PowerShell)
- Simple and minimal interface
- Paste, open, or load a structure from a file
- Preview the detected project structure
- Copy generated code instantly
- Save the generated script to your computer

##  How to Use

### 1. Get a project structure

Ask an AI to generate a folder structure for your project.

Example:

```text
website/
├── public/
│   ├── images/
│   └── favicon.ico
├── src/
│   └── main.js
└── package.json
```

### 2. Paste it into Aifolix

Paste the structure into the **Project Structure** box.

You can also use **Paste**, **Open**, or drag & drop a supported `.txt`, `.tree`, or `.md` file.

### 3. Choose your shell

Select the desired output language from the **Language** dropdown.

### 4. Generate

Click **Generate**.

Aifolix detects the folders and files and converts them into a script that can recreate the structure automatically.

### 5. Use the generated script

Copy the generated code or click **Save** to save it as a script file.

Then, save or copy the script into your project folder and run it:

- **Windows (CMD):** double-click the `.bat` file.
- **PowerShell:** right-click the `.ps1` file → "Run with PowerShell".

Now your complete folder and file structure will be created automatically.

##  Built With

- HTML
- CSS
- JavaScript

##  Why Aifolix?

Creating a project structure manually is repetitive and wastes time.

Aifolix turns this:

```text
AI-generated structure
        ↓
      Aifolix
        ↓
   Shell script
        ↓
Complete project structure
```

into a quick and repeatable workflow.

##  Roadmap

Aifolix is still in active development.  
Planned for future versions:

- ⏳ Ready-to-use installers for all platforms

More great updates are coming soon.

## Project Status

**Version:** 1.4  
**Status:** Active Development

Aifolix is my first public GitHub project.

---

**Copyright © 2026 Yasin Jalilpour. All rights reserved.**
